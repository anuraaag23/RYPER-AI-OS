# RYPER AI OS — v0.1.1 Release Candidate Report

**Release Candidate Version:** `0.1.1`  
**Date:** September 8, 2026  
**Source Commit Before Release:** `63c862107ba8d11e4f50720e1a4906c731608c20`  
**Frozen v0.1.0 Commit:** `63c862107ba8d11e4f50720e1a4906c731608c20` (Tag: `v0.1.0`) — **UNTOUCHED**  
**Frozen v0.1.0 Installer SHA-256:** `0894ED2797E89AF0B5AD923749D5731BB73034D3C058B7C5FCFAADCDD8E5DA2B` — **EXACT MATCH**  
**Release Candidate Installer:** `platform/desktop-app/dist-release/RYPER AI OS Setup 0.1.1.exe`  
**Release Candidate SHA-256:** `BAFB215CAF0763F5FCDCDB9DD01E750FEB08BF0C50054610E10131B988F61267`  
**Release Candidate Size:** `101,668,321` bytes (~96.96 MiB)  
**Target Platform / Architecture:** Windows 10/11 x64 (`win32-x64`, NSIS)  
**Validation Status:** **Release candidate validated locally; GitHub publication not yet performed.**

---

## 1. Executive Summary

This document certifies the packaging, verification, and end-to-end acceptance of the **RYPER AI OS v0.1.1 Release Candidate**.

The v0.1.1 release consolidates all user-experience fixes and polish from milestones P0, P1, P2, and P3, including onboarding guidance, push-to-talk shortcut discoverability, local AI status badges and privacy explanations, human-friendly error sanitization, diagnostics health reporting with system clipboard sanitization, and honest release channel notices.

The candidate was built using the official electron-builder packaging pipeline, verified against all 1,648 automated unit and integration tests, and evaluated via real runtime dynamic testing against the **actual installed application binary** (`RYPER AI OS.exe`, FileVersion `0.1.1`) installed from `RYPER AI OS Setup 0.1.1.exe`.

---

## 2. Monorepo Test & Build Results

### Automated Regression Suite
- **Command:** `npm test`
- **Test Files:** 239 total (233 passed, 6 skipped)
- **Individual Tests:** 1,661 total (**1,648 passed**, 13 skipped, **0 failed**)
- **Duration:** 16.97 seconds
- **Failures / Retries:** 0

### Production Build
- **Command:** `npm run build`
- **Output:**
  - `tsc --build`: Zero compile diagnostics.
  - `vite build`: Production renderer bundle in `dist-renderer/` (main bundle: 161.5 kB, settings bundle: 15.1 kB, base CSS: 21.2 kB).
  - `esbuild preload`: Bundle in `dist-electron/preload.cjs` (7.8 kB).
  - Build duration: 873 ms. Zero warnings or errors.

---

## 3. Package & Installer Specification

| Property | Value |
|---|---|
| **Package Name** | `@ryper/desktop-app` |
| **Product Name** | `RYPER AI OS` |
| **Version** | `0.1.1` |
| **Installer File** | `RYPER AI OS Setup 0.1.1.exe` |
| **File Size** | `101,668,321` bytes |
| **SHA-256 Checksum** | `BAFB215CAF0763F5FCDCDB9DD01E750FEB08BF0C50054610E10131B988F61267` |
| **Checksum File** | `platform/desktop-app/dist-release/SHA256SUMS-v0.1.1.txt` |
| **Installer Type** | Nullsoft Scriptable Install System (NSIS x64) |
| **Target Architecture** | Windows x64 |
| **ASAR Packaging** | Enabled (`resources/app.asar`, 14,620,897 bytes) |
| **Excluded from ASAR** | TypeScript source files, source maps (`.map`), and test suites |
| **PE FileVersion** | `0.1.1` |
| **PE ProductVersion** | `0.1.1.0` |

---

## 4. Actual Installed Application Acceptance

Acceptance testing was performed against the **actual installed application** (`C:\RyperAI\v0.1.1-test-install\RYPER AI OS.exe`, 225,446,912 bytes) installed via `RYPER AI OS Setup 0.1.1.exe /S`:

| Acceptance Domain | Tested Feature | Result | Forensic Evidence |
|---|---|:---:|---|
| **First-Run UX** | 3-Step Onboarding Modal | **PASS** | Modal displays cleanly on first launch; completes and persists `hasCompletedOnboarding: true`. |
| **Composer & Chips** | Initial Prompt Readiness | **PASS** | Composer input is immediately interactive; suggestion chips rendered. |
| **Local AI Status** | Status Indicator Badge | **PASS** | Evaluates to `ready` when connected to local `llama-server.exe` (`Qwen3-8B-Q4_K_M.gguf`, ctx=8192). |
| **Local Inference** | Live Chat Turn | **PASS** | Dispatched prompt answered cleanly ("Hello!"). Streamed completion verified. |
| **Cancellation** | Turn Cancellation | **PASS** | In-flight turn cleanly cancelled via `window.ryper.cancelTurn()` with `cancelled: true`. |
| **Voice Shortcuts** | Push-to-Talk Tooltip | **PASS** | VoiceOrb and Composer mic buttons display `"Ctrl + Shift + Space"` shortcut. |
| **Windows Tools** | Capability Broker | **PASS** | Read tools execute safely; mutating capabilities require user consent. |
| **Diagnostics** | Health & System Info | **PASS** | 5 subsystem health cards reported healthy; capability count: 18. |
| **Diagnostics Copy** | System Clipboard Sanitization | **PASS** | Version reported as `0.1.1`; zero usernames (`anura`), user paths, or tokens exposed. |
| **Settings Persistence** | Update & Store Settings | **PASS** | Setting updates (`theme: 'dark'`, `voiceLanguage: 'hi'`) persist across restarts in `settings.json`. |

---

## 5. Security Smoke Test (Installed Application)

The packaged runtime environment was audited against security baselines:

| Security Property | Expected | Observed in Installed Binary | Status |
|---|---|---|:---:|
| **Context Isolation** | `true` | `window.process === undefined` | **PASS** |
| **Node Integration** | `false` | `window.require === undefined`, `window.Buffer === undefined` | **PASS** |
| **Sandbox** | `true` | Renderer sandbox active | **PASS** |
| **API Exposure** | Whitelisted | Only `window.ryper` and `window.ryperAudioBridge` exposed | **PASS** |
| **Prototype Defense** | Resistant | Prototype pollution cannot elevate renderer privileges | **PASS** |
| **Path Access** | Sandboxed | Direct arbitrary filesystem / shell execution unavailable | **PASS** |

---

## 6. Install, Uninstall & Reinstall Verification

The lifecycle of the v0.1.1 installer package was tested on Windows:

1. **Clean Installation:** `RYPER AI OS Setup 0.1.1.exe /S /D=C:\RyperAI\v0.1.1-test-install` installed all application binaries, locales, and resources.
2. **User Data Modification:** Modified user preferences (`theme: 'dark'`, `voiceLanguage: 'hi'`) saved to user data profile.
3. **Uninstallation:** Executed `Uninstall RYPER AI OS.exe /S _?=C:\RyperAI\v0.1.1-test-install`.
   - Result: `RYPER AI OS.exe` and program directory completely uninstalled (`Test-Path` returned `False`).
   - User data preservation: User settings in `%APPDATA%` remained intact (`settings.json` preserved).
4. **Reinstallation:** Reinstalled `RYPER AI OS Setup 0.1.1.exe /S /D=C:\RyperAI\v0.1.1-test-install`.
   - Result: `RYPER AI OS.exe` restored with ProductVersion `0.1.1.0`.
   - Previous settings (`theme: 'dark'`, `voiceLanguage: 'hi'`) immediately loaded on launch.

---

## 7. Frozen Baseline Integrity

Verification confirmed that the frozen v0.1.0 release baseline and installer remain completely unchanged:

```powershell
git rev-parse "v0.1.0^{commit}"
# Output: 63c862107ba8d11e4f50720e1a4906c731608c20 (EXACT MATCH)

Get-FileHash -Algorithm SHA256 "platform\desktop-app\dist-release\RYPER AI OS Setup 0.1.0.exe"
# Algorithm: SHA256
# Hash:      0894ED2797E89AF0B5AD923749D5731BB73034D3C058B7C5FCFAADCDD8E5DA2B (EXACT MATCH)
```

---

## 8. Documented Known Limitations

1. **Wake Word Detection:** The wake-word subsystem uses an energy-based audio threshold fallback. Continuous on-device deep-learning wake-word detection is **NOT independently certified**.
2. **TTS Acoustic Verification:** Piper TTS speech generation is validated at the bridge and audio-file level; perceptual acoustic fidelity across various speaker outputs has **NOT been independently certified by human hearing**.
3. **P1 Test 3 (Completed-Action Retry Safety):** Remains documented as *"Not independently verified — implementation covered by unit tests."*
4. **Local Qwen3-8B Hybrid GPU/CPU Prefill Latency:** On systems with limited free GPU VRAM, initial tool-schema prompt prefills (over 7,500 tokens) execute via hybrid CPU offload, requiring 70–120 seconds on the first cold turn before KV prefix caching takes effect. Subsequent turns execute in 1–3 seconds.
5. **Plugin Isolation:** In-process plugins execute within the trusted application runtime and are **NOT sandboxed for untrusted third-party code**.
6. **Settings Storage:** Local application preferences (`settings.json`) are stored as plaintext JSON in `%APPDATA%\ryper-desktop`, not encrypted with Windows DPAPI.
7. **Code Signing:** Windows installer binaries are not signed with an EV/OV Authenticode certificate. Windows SmartScreen warnings are expected on initial download.
8. **Update Delivery:** There is no silent in-app auto-update mechanism; updates require manual download from GitHub releases.

---

## 9. Release State & Status

- **Status:** **Release candidate validated locally; GitHub publication not yet performed.**
- **Commit:** Ready for release commit (`release: RYPER AI OS v0.1.1`).
- **Tag:** Ready for annotated tag (`v0.1.1`).
