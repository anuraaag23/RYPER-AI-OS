// Entry point placeholder for the iOS/iPadOS shell library target.
// Intentionally minimal: this file exists so `Package.swift` describes a
// buildable target once opened on macOS with Xcode. The real SwiftUI app
// (scenes, WidgetKit extension, Core IPC client) lands once this shell's
// implementation phase begins, per the roadmap.
public struct RyperAppInfo {
    public static let name = "RYPER AI OS — iOS shell scaffold."
}
