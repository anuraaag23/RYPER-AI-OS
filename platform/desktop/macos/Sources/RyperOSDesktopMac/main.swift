// Entry point placeholder for the macOS shell executable target.
// Intentionally minimal: this file exists so `Package.swift` describes a
// buildable target once opened on macOS with Xcode's command line tools.
// The real AppKit/SwiftUI app (windows, menu bar item, Core IPC client)
// lands once this shell's implementation phase begins, per the roadmap.
print("RYPER AI OS — macOS shell scaffold. Implementation begins in a later phase.")
