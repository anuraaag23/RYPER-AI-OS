// Entry point placeholder for the GTK4 Linux shell binary.
// Intentionally minimal: proves out the Cargo manifest's shape. The real
// GTK4 application (window, Glass Dock, Core gRPC client) lands once this
// shell's implementation phase begins, per the roadmap.
fn main() {
    println!("RYPER AI OS — Linux shell scaffold. Implementation begins in a later phase.");
}
